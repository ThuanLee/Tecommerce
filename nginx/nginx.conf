server {
    listen 80;
    server_name 18.163.214.219;

    location / {
        try_files $uri $uri/ /index.html;
        root /static;
    }

    location /api {
        proxy_pass http://backend:8000;
    }
}